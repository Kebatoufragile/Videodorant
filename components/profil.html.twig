{% extends 'base.html.twig' %}

{% block content %}

{% if success != false %}
<div class="card-panel green accent-3">
  {{success}}
</div>
{% endif %}

<div class="col s12 m8 offset-m2 l6 offset-l3">
  <div class="center-align">
    <img src="img/hqdefault.jpg" alt="profil_pic" class="circle responsive-img">
  </div>
  <div class="center-align">
    Bienvenue sur le profil de {{user.username}}
  </div>
</div>

{% endblock %}
