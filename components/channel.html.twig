{% extends 'base.html.twig' %}

{% block content %}

    <div class="row">

        <div class="col s10 push-l1 card blue-grey darken-2 z-depth-5 center-align">
            <img src="assets/img/profil/{{ user.profilPicLink }}" class="circle" style="max-height:150px"/>
            <h3 class="card-title">Chaîne de <strong>{{ user.username }}</strong></h3>
        </div>

    </div>

{% endblock %}